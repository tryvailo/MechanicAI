# 🔍 Отладка на мобильных устройствах

## Способ 1: Встроенный компонент отображения ошибок (Рекомендуется) ⭐

В приложении добавлен компонент `ErrorDisplay`, который автоматически показывает все ошибки на экране:

- **Расположение**: Правый нижний угол экрана
- **Автоматически перехватывает**:
  - Ошибки JavaScript (`console.error`)
  - Необработанные ошибки (`window.onerror`)
  - Отклоненные промисы (`unhandledrejection`)
- **Функции**:
  - Показывает последние 50 ошибок
  - Автоматически сохраняет в localStorage (сохраняются даже после перезагрузки!)
  - Можно развернуть/свернуть
  - Показывает stack trace
  - Время возникновения ошибки
  - Кнопка "Поделиться" для отправки ошибок
  - Кнопка "Открыть страницу отладки" для детального просмотра

**Использование**: Просто откройте приложение на телефоне - ошибки будут видны автоматически!

---

## Способ 2: Страница отладки `/debug` (Без подключения к компьютеру!) 🎯

**Самый простой способ для мобильных устройств!**

1. Откройте приложение на телефоне
2. В адресной строке добавьте `/debug` (например: `https://your-app.vercel.app/debug`)
3. Или нажмите кнопку "Открыть страницу отладки" в компоненте ошибок

**Что доступно на странице отладки:**
- ✅ Просмотр всех сохраненных ошибок (до 50 последних)
- ✅ Детальная информация по каждой ошибке (stack trace, URL, user agent)
- ✅ Экспорт всех ошибок в текстовый файл
- ✅ Поделиться ошибками через системное меню "Поделиться"
- ✅ Копирование отдельных ошибок
- ✅ Очистка всех ошибок

**Преимущества:**
- Не требует подключения к компьютеру
- Работает на любом устройстве
- Ошибки сохраняются даже после перезагрузки страницы
- Можно отправить ошибки разработчику через "Поделиться"

---

## Способ 2: Chrome DevTools (Android)

### Подключение через USB:

1. **Включите USB отладку на Android**:
   - Настройки → О телефоне → Нажмите 7 раз на "Номер сборки"
   - Настройки → Для разработчиков → USB отладка (включить)

2. **Подключите телефон к компьютеру через USB**

3. **Откройте Chrome на компьютере**:
   - Перейдите на `chrome://inspect`
   - Включите "Discover USB devices"
   - Ваше устройство появится в списке

4. **Откройте приложение на телефоне** и нажмите "Inspect" в Chrome

5. **Откройте Console** для просмотра логов и ошибок

---

## Способ 3: Safari Web Inspector (iOS)

### Подключение через USB:

1. **На iPhone/iPad**:
   - Настройки → Safari → Дополнения → Веб-инспектор (включить)

2. **На Mac**:
   - Откройте Safari
   - Safari → Настройки → Дополнения → Показать меню "Разработка" в строке меню

3. **Подключите iPhone к Mac через USB**

4. **В Safari на Mac**:
   - Разработка → [Ваш iPhone] → [URL вашего приложения]

5. **Откройте Console** для просмотра логов

---

## Способ 4: Удаленная отладка через Chrome (Android/iOS)

### Для Android:

1. Откройте Chrome на телефоне
2. Перейдите на `chrome://inspect`
3. Включите "Discover network targets"
4. На компьютере откройте `chrome://inspect` и подключитесь

### Для iOS (требует Safari на Mac):

1. Подключите iPhone к Mac
2. Откройте Safari на Mac
3. Разработка → [Ваш iPhone] → [URL]

---

## Способ 5: Vercel/Next.js логи (для продакшн)

Если приложение задеплоено на Vercel:

1. Откройте [Vercel Dashboard](https://vercel.com)
2. Выберите ваш проект
3. Перейдите в **Deployments** → выберите деплой
4. Откройте **Functions** → выберите API route
5. Просмотрите **Logs** для ошибок сервера

---

## Способ 6: Локальное логирование в файл

Для более детального логирования можно добавить отправку ошибок на сервер:

```typescript
// В компоненте ErrorDisplay можно добавить:
const sendErrorToServer = async (error: ErrorInfo) => {
  try {
    await fetch('/api/log-error', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(error),
    });
  } catch (e) {
    // Ignore
  }
};
```

---

## Быстрые команды для отладки

### Показать все ошибки в консоли:
```javascript
// В консоли браузера на телефоне (если доступна)
console.error('Test error');
```

### Проверить наличие ошибок:
```javascript
// В консоли
window.onerror = (msg, url, line) => {
  console.error('Error:', msg, 'at', url, ':', line);
};
```

---

## Полезные ссылки

- [Chrome Remote Debugging](https://developer.chrome.com/docs/devtools/remote-debugging/)
- [Safari Web Inspector](https://developer.apple.com/documentation/webkit/safari_web_inspector)
- [Next.js Error Handling](https://nextjs.org/docs/app/building-your-application/routing/error-handling)

---

## Советы

1. **Используйте встроенный ErrorDisplay** - самый простой способ для мобильной отладки
2. **Для детальной отладки** - используйте Chrome DevTools или Safari Web Inspector
3. **Для продакшн ошибок** - проверяйте логи Vercel
4. **Добавьте логирование** - отправляйте критические ошибки на сервер для анализа

