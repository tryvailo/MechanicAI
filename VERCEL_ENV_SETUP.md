# 🔧 Настройка Environment Variables для Vercel

## 📋 Обязательные переменные

### Для Oil Change Assistant и Realtime Dashboard:

#### `NEXT_PUBLIC_GEMINI_API_KEY` 🔥 (Для /oil-change и /realtime)
- **Описание**: API ключ Google Gemini для интерактивного инструктажа по замене масла и диагностики в реальном времени
- **Где получить**: https://aistudio.google.com/app/apikey
- **Формат**: `AIzaSy...`
- **Используется для**:
  - `/oil-change` - интерактивный инструктор по замене масла
  - `/realtime` - диагностика в реальном времени через видеопоток
- **Важно**: Это публичная переменная (NEXT_PUBLIC_), она доступна в браузере

### Минимум один из двух API ключей (для работы основного приложения):

#### 1. `OPENAI_API_KEY` ⭐ (Рекомендуется)
- **Описание**: API ключ OpenAI для анализа фото, транскрипции голоса и чата
- **Где получить**: https://platform.openai.com/api-keys
- **Формат**: `sk-proj-...`
- **Используется для**:
  - `/api/analyze-photo` - анализ фото (GPT-4 Vision)
  - `/api/transcribe` - транскрипция голоса (Whisper)
  - `/api/chat` - чат с ассистентом (GPT-4)

#### 2. `CLAUDE_API_KEY` (Альтернатива OpenAI)
- **Описание**: API ключ Anthropic/Claude для анализа фото и чата
- **Где получить**: https://console.anthropic.com/settings/keys
- **Формат**: `sk-ant-api03-...`
- **Используется для**:
  - `/api/analyze-photo` - анализ фото (Claude Vision)
  - `/api/chat` - чат с ассистентом (Claude)

> ⚠️ **Важно**: Для работы `/api/transcribe` (транскрипция голоса) **обязательно** нужен `OPENAI_API_KEY`, так как Whisper API доступен только через OpenAI.

## 🎛️ Опциональные переменные (настройки)

### Выбор провайдера API:

#### `VISION_API_PROVIDER`
- **Описание**: Выбор провайдера для анализа фото
- **Значения**: `openai` или `claude`
- **По умолчанию**: `openai`
- **Пример**: `openai`

#### `CHAT_API_PROVIDER`
- **Описание**: Выбор провайдера для чата
- **Значения**: `openai` или `claude`
- **По умолчанию**: `openai`
- **Пример**: `openai`

### Переопределение моделей (опционально):

#### `OPENAI_MODEL`
- **Описание**: Модель OpenAI для использования
- **По умолчанию**: `gpt-4o`
- **Примеры**: `gpt-4o`, `gpt-4-turbo`, `gpt-4`

#### `CLAUDE_MODEL`
- **Описание**: Модель Claude для использования
- **По умолчанию**: `claude-3-5-sonnet-20241022`
- **Примеры**: `claude-3-5-sonnet-20241022`, `claude-3-opus-20240229`

## 📝 Инструкция по настройке в Vercel

### Шаг 1: Откройте настройки проекта
1. Перейдите на https://vercel.com
2. Выберите ваш проект **AutoDoc Mechanic AI**
3. Откройте **Settings** → **Environment Variables**

### Шаг 2: Добавьте переменные

#### Минимальная конфигурация (только OpenAI):
```
OPENAI_API_KEY = sk-proj-your-openai-key-here
```

#### Конфигурация с Gemini (для Oil Change Assistant):
```
NEXT_PUBLIC_GEMINI_API_KEY = AIzaSy-your-gemini-key-here
OPENAI_API_KEY = sk-proj-your-openai-key-here
```

#### Полная конфигурация (OpenAI + Claude + Gemini):
```
OPENAI_API_KEY = sk-proj-your-openai-key-here
CLAUDE_API_KEY = sk-ant-api03-your-claude-key-here
VISION_API_PROVIDER = openai
CHAT_API_PROVIDER = openai
```

#### Конфигурация с кастомными моделями:
```
OPENAI_API_KEY = sk-proj-your-openai-key-here
CLAUDE_API_KEY = sk-ant-api03-your-claude-key-here
VISION_API_PROVIDER = openai
CHAT_API_PROVIDER = claude
OPENAI_MODEL = gpt-4o
CLAUDE_MODEL = claude-3-5-sonnet-20241022
```

### Шаг 3: Выберите окружения
Для каждой переменной выберите окружения, где она будет доступна:
- ✅ **Production** - для продакшн деплоя
- ✅ **Preview** - для preview деплоев (pull requests)
- ✅ **Development** - для локальной разработки (опционально)

### Шаг 4: Сохраните и передеплойте
1. Нажмите **Save**
2. Перейдите в **Deployments**
3. Нажмите **Redeploy** на последнем деплое (или создайте новый)

## 🔍 Проверка конфигурации

После деплоя проверьте, что переменные загружены:

1. Откройте **Deployment** → **Functions** → выберите любой API route
2. Проверьте логи - не должно быть ошибок `API key not configured`
3. Протестируйте endpoints:
   - `/api/analyze-photo` - загрузите фото
   - `/api/transcribe` - отправьте аудио
   - `/api/chat` - отправьте сообщение

## ⚠️ Важные замечания

1. **Безопасность**:
   - ✅ Никогда не коммитьте реальные ключи в git
   - ✅ Используйте только Environment Variables в Vercel
   - ✅ Регулярно ротируйте ключи

2. **Стоимость**:
   - OpenAI GPT-4 Vision: ~$0.01-0.03 за изображение
   - OpenAI Whisper: ~$0.006 за минуту аудио
   - OpenAI GPT-4o: ~$0.005-0.015 за 1K токенов
   - Claude: аналогичные цены

3. **Лимиты**:
   - Установите лимиты использования в панелях OpenAI и Anthropic
   - Мониторьте использование через Vercel Analytics

## 📊 Рекомендуемые конфигурации

### Для MVP/Тестирования:
```
OPENAI_API_KEY = (ваш ключ)
VISION_API_PROVIDER = openai
CHAT_API_PROVIDER = openai
```

### Для продакшна с резервированием:
```
OPENAI_API_KEY = (ваш ключ)
CLAUDE_API_KEY = (ваш ключ)
VISION_API_PROVIDER = openai
CHAT_API_PROVIDER = openai
```

### Для максимальной гибкости:
```
OPENAI_API_KEY = (ваш ключ)
CLAUDE_API_KEY = (ваш ключ)
VISION_API_PROVIDER = openai
CHAT_API_PROVIDER = claude
OPENAI_MODEL = gpt-4o
CLAUDE_MODEL = claude-3-5-sonnet-20241022
```

## 🆘 Troubleshooting

### Ошибка: "API key not configured"
- ✅ Проверьте, что переменная добавлена в Vercel
- ✅ Проверьте, что выбраны правильные окружения (Production/Preview)
- ✅ Передеплойте проект после добавления переменных

### Ошибка: "Invalid API key"
- ✅ Проверьте правильность ключа (без пробелов, полный ключ)
- ✅ Убедитесь, что ключ активен в панели провайдера
- ✅ Проверьте лимиты использования ключа

### Функции не работают после деплоя
- ✅ Проверьте логи в Vercel Dashboard
- ✅ Убедитесь, что переменные добавлены для всех окружений
- ✅ Перезапустите деплой

