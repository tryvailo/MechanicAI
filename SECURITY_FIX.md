# 🔒 Исправление проблемы с API ключами

## Проблема
GitHub Secret Scanning обнаружил реальные API ключи в коммите и заблокировал push.

## Решение
✅ **Исправлено:**
- Удалены реальные API ключи из `ENV_SETUP.md`
- Удалены реальные API ключи из `setup-env.sh`
- Заменены на placeholder значения: `sk-proj-your-openai-api-key-here` и `sk-ant-api03-your-claude-api-key-here`
- Создан новый чистый коммит без секретов

## ⚠️ ВАЖНО: Что нужно сделать

### 1. Отзовите старые ключи (если они были в коммите)
Если ключи попали в публичный репозиторий, их нужно отозвать:

- **OpenAI**: https://platform.openai.com/api-keys
- **Anthropic**: https://console.anthropic.com/settings/keys

### 2. Создайте новые ключи
После отзыва создайте новые ключи и добавьте их в `.env.local` (который НЕ коммитится в git).

### 3. Безопасное хранение ключей
- ✅ `.env.local` - в `.gitignore`, не коммитится
- ✅ Используйте placeholder в документации
- ✅ Для Vercel: добавляйте ключи через Settings → Environment Variables

## Текущий статус
- ✅ Все файлы готовы к push без секретов
- ✅ Новый коммит создан без API ключей
- ✅ Можно безопасно делать push в GitHub

